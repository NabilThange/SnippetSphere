# Strict peer dependencies
strict-peer-dependencies=false

# Save exact versions
save-exact=true

# Enable auto-install of peer dependencies
auto-install-peers=true

# Prefer offline mode to reduce network requests
prefer-offline=true 